integer factorial(integer n){
    integer f := 1;
    integer i := n;

    if i > 0 then {
        do {
            f := f * i
            i := i - 1
        } while i > 0;
    };
    
    factorial := f
};
